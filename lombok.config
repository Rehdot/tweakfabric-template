# TweakFabric does not have access to lombok sources at runtime, so we should always
# disable lombok from adding @Generated in order to compile classes that use lombok.
lombok.addLombokGeneratedAnnotation = false